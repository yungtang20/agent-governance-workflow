# Contributing

Keep changes small and evidence-based.

- Preserve the distinction between `REVIEWED_PIPELINE` and `SINGLE_EXECUTOR`.
- Use capability-based roles instead of hard-coding vendor model names.
- Add negative tests for new validation rules.
- Do not add machine-specific paths or real operational records.
- Run the validator and unit tests before submitting changes.
